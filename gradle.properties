# JVM 堆内存配置
org.gradle.jvmargs=-Xmx8048m -Dfile.encoding=UTF-8

# 启用并行编译
org.gradle.parallel=true

# 启用配置缓存（首次构建需下载依赖，暂不启用）
# org.gradle.configuration-cache=true

# 使用 AndroidX
android.useAndroidX=true

# R 类命名空间化，减小 R 类体积
android.nonTransitiveRClass=true
