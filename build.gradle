// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '8.9.0-rc01' apply false
    id 'com.android.library' version '8.9.0-rc01' apply false
    id 'org.jetbrains.kotlin.android' version '2.1.0' apply false4
    id "org.jetbrains.kotlin.plugin.compose" version "1.8.22" // 使用与 Kotlin 版本匹配的插件版本
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
